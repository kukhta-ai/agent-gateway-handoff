# Changelog

All notable changes to this project are documented here. The format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and the project adheres to
[Semantic Versioning](https://semver.org/spec/v2.0.0.html) (see
[`CONTRIBUTING.md` → Versioning & releases](./CONTRIBUTING.md#versioning--releases)).

## [Unreleased]

The **scenario-01 MVP is complete** — all 66 backlog tasks done, 428 tests green, the full same-session browser-handoff thread (Phases E,0–15) proven end-to-end through the real modules, built slice by slice through the BMAD SDLC with a security review on every seam.

The **authentik delegated-identity-provider integration is complete** — all 10 follow-on tasks (GLA-067–076) done, 548 tests green. authentik is integrated as a delegated auth provider **behind the unchanged `AuthProviderPort` seam**, so a recipient can authenticate with a **phishing-resistant passkey or a typed-password fallback** (both hosted by authentik) and the gateway gates by required strength — selecting it changes no gateway or core code, only a new adapter and the composition wiring. The real OIDC interop was **proven against a real authentik 2025.10.4** stood up by the integration's own installer.

The **`wpm` installer project has been re-authored against the new wpm "authoring-context" model.** `wpm/` is now a proper **authoring workspace** — the shipped deliverable lives under `wpm/wip/` (built to `wpm/builds/gla-<version>.tgz`), with the authoring tracker (`.authoring-backlog/`) and build output gitignored. Each of the six bundles (gla-core, browser-runtime, human-view, isolation, edge-proxy, identity-provider) now carries the new conventions: **V2 recipe-task tags** (`kind:` / `step:` labels + a version milestone), a per-bundle **pull-UX advisor**, an `_AGENTS.md` executor front door (renamed to `AGENTS.md` + per-target aliases at build), and the **auto-rendered `gla-installer` orchestrator**. All proven content — the authentik `amr` scope-mapping recipe, the standup probes and skill, the `connect-and-drive-gla` skill, and the service/env/Caddy templates — was carried over **byte-identical**, and per-bundle acceptance-criteria + Definition-of-Done counts match the prior installer exactly.

### Added

- **Architecture & design set** — `docs/architecture/{baseline,kernel-contracts,dependency-strategy,test-strategy}.md`: the concrete TS/Node modular-monolith realization of the committed spec, the kernel contracts, the dependency/ownership map, and the test architecture (scenario-01 E2E harness + 10 security invariants).
- **Scaffold & quality gate** — a pnpm-workspace modular monolith (core / core-adjacent / edge / adapter / surface rings) with the import boundary enforced by both the package graph and a Biome rule; `pnpm gate` (`tsc -b` + `biome ci .` + `vitest run`) wired into pre-commit/CI/DoD, with a self-test proving a deliberate violation fails the gate.
- **Kernel** (`@gla/kernel`, zero runtime deps) — core entities + state machines (typed invalid-transition errors); the capability primitive (signing-independent port + reference HMAC signer: mint/attenuate/stateless verify, recipient-binding fail-closed, signed lineage with full-ancestor revocation, scope fail-closed); offline `AssemblySpec` validation (all defects in one pass); the typed `config_schema` validator; and the stable error→exit-code taxonomy (0–8, incl. `mount.*`).
- **`gla` CLI** (the Agent Bridge surface) — `whoami`, `template list|show`, `skill list|show`, `catalog list`, `task create|get|list`, `session create [--dry-run]`; JSON to stdout by default (human text only at a TTY), with the documented exit-code contract (0 / 2 usage / 3 policy / 4 authz / 5 not-found / 7 conflict / 8 dependency).
- **Inbound + orient (Phases 0–1)** — `channel-cli` (ChannelPort) delivering an inbound message with its recipient binding; the CapabilityService (agent-authority anchor + verify-first `whoami`); the Catalog (Store→Ingester→Index with system-derived availability; the `browser-handoff` template + provider manifests + a skill); recipient identity binding.
- **Propose + admit (Phase 2)** — the Task service (durable task root; task capability attenuated from the agent-authority); the assembly resolver (template overlay → resolved spec); the admission **mutate→validate** pipeline (offline policy / capability-scope / catalog-availability / config-schema / mount checks, minting and running nothing, so a `--dry-run` matches the real run); and the embedded **Cedar** policy engine (forbid-wins, deterministic, fail-closed) behind the policy port.
- **Provision the live capsule + agent connector (Phases 3–4)** — the worker plane (pluggable Spawner Registry + per-launcher mount capability, capsule lifecycle, workspace manager, idempotent cleanup reconciler); a real **process-tier launcher** (`launcher-process`: headless Chromium via Playwright for the CDP path, upgrading to headed Chromium + Xvfb + x11vnc + websockify/noVNC where available); the reversible provision saga; and an **agent-blind CDP connector** (`secret_ref` is a capability reference, not a raw secret) that descends from the task capability (lineage-revoke cascades) and is revoked + unbound at teardown. `gla session create` now provisions a real capsule.
- **Recipient enrollment (Phase E)** — the **Access Gateway** (the sole public entry, an HTTP server fronting enrollment) admitting only on a verified **single-use `operator-discharge` grant** (absent/forged/wrong-recipient/expired/reused refused, atomically consumed); the in-tree **WebAuthn** AuthProvider (`@simplewebauthn`) registering a passkey bound to a recipient identity (no half-bound state on failure); and the operator `enrollInvite` delivering the invite over the channel.
- **The handoff — open window → verify-at-edge → reach the capsule (Phases 5–6)** — `gla handoff open` mints a **recipient-bound, short-TTL `session` grant** (attenuated from the connector, never widened), programs a grant-bound **Route** on the gateway, and delivers the link; the gateway **verifies the grant statelessly** and requires the bound identity (**WebAuthn step-up** against the enrolled credential) before forwarding; on success it **proxies the authorized WebSocket** to the capsule's noVNC entrypoint and nothing else (SSRF-closed: the upstream is the verified route, never request data), with connect/idle timeouts and stripped handshake headers; a **revoked or expired grant force-closes the live connection**. `gla handoff wait|get|list|cancel` added.
- **Work agent-blind → completion → close (Phases 7–8)** — the **agent-blind invariant is enforced**: the agent's CDP is tunnelled through a GLA-side **broker**, and opening a handoff window **severs the agent's live connection** (and rewrites `/json` so the real port never leaks), so the human's secret entry is unreadable by the agent (a known single-host loopback limitation is documented); the **Completion service** validates a detector signal against its contract and normalizes it to an envelope (out-of-contract rejected; the capsule emits, never decides); the **url-watcher** detector fires on the configured URL; and a validated completion (or TTL expiry) **closes the window** — unmount route, revoke grant, force-close the WS — returning the session to `active` with the **capsule still running**, so `gla handoff wait` returns the completion envelope and the agent resumes.
- **The second handoff (Phases 9–14)** — the agent inspects `/verify` over its resumed connector, **re-opens a second window on the same capsule** (no re-provision), the human enters the verification code agent-blind, the url-watcher completes on `/dashboard` (`verified`), and the agent configures the account — proven by a real two-handoff end-to-end test. The one new mechanism is **auth reuse on re-open**: a recipient already authenticated within a TTL is not re-prompted for WebAuthn (recipient-bound to the signed grant, the grant still cryptographically verified every time). Most "without rebuilding" steps were verified to reuse the existing machinery unchanged.
- **Teardown (Phase 15)** — `gla task complete` / `gla task revoke` drive one terminal Task transition that tears down every session (cancel any open window, **stop the capsule** and reap the ephemeral workspace) and performs a **single revoke of the task capability** so — by signed lineage — every descendant (session grants, the agent connector) stops verifying. Verified against a real capsule: the process is gone, the temp profile deleted, the route unmounted, the caps `auth.revoked`, **host-mounted paths and persisted outputs survive**, and re-running teardown is idempotent with the reconciler confirming no orphan.
- **Scenario-01 end-to-end (the MVP capstone)** — a single **cold** test drives the entire scenario-01 thread (Phases E,0–15) through the real modules with real headless Chromium, a real WebAuthn ceremony (virtual authenticator), the agent-blind CDP broker, and a stub target site — asserting the full behaviour plus the security invariants (recipient fail-closed, agent-blind, stateless verify, offline-reject exit codes, out-of-contract rejection, forwarded-link-useless). The import boundary holds and a provider swap (channel) runs through the thread with no core change. **The MVP is proven.**
- **Deployable as a service** — `gla serve` binds the Access Gateway on `:3000` (the public entry, behind Caddy) and the Agent Bridge on a local socket; the `gla` CLI drives the running daemon's shared state via `GLA_ENDPOINT`, and `--public-base-url` makes handoff/enrollment links reachable. Graceful shutdown tears down live capsules with no orphan.
- **Agent-native installer** — a `wpm/` bundle-project (gla-core + browser-runtime, human-view, isolation, edge-proxy, identity-provider) that an operator's agent runs to stand GLA up on a host (detect → setup → verify → record receipt per bundle); `wpm project validate` and `build dry-run` pass.
- **authentik delegated identity provider (GLA-067–076)** — a delegated **OIDC** auth provider (`@gla/auth-authentik`, authorization-code + PKCE via `jose`) behind the **unchanged** `AuthProviderPort`: it validates the `id_token` (JWKS-pinned RS256/ES256, `iss`/`aud`/`azp`/`nonce`/`exp`), binds a recipient to a **stable provider subject** (`sub`), and reports the same `{ok, authStrength}` fact the in-tree provider does — passkey → `webauthn`, password → `password` (never up-mapped). The **gateway pages were generalized provider-agnostically** (a generic `options.kind === "redirect"` branch in the served step-up/enroll pages + an adapter-owned callback that re-POSTs `{code,state}` to the **byte-for-byte unchanged** verify routes; the GLA grant is held in same-origin `sessionStorage` and never reaches authentik), so the **same gateway code path serves both providers** and the existing `requiredAuthStrength` gate admits a passkey / refuses a password-only step-up where the stronger method is required. The in-tree WebAuthn provider **stays the default**; authentik is opt-in via `GLA_AUTH_PROVIDER=authentik` + the `GLA_AUTHENTIK_*` OIDC config. A `wpm` installer (the `identity-provider` bundle's authentik branch) stands authentik up (or adopts it) **host-level** — the hermes-1 reference, since nested Docker can't run a Compose stack in-container — configures the OIDC application + a passkey/password flow that emits a distinguishing `amr`, and records a `DependencyBinding` receipt (client secret as a secret-ref). The crown-jewel seams (the OIDC adapter, the dual-method step-up) were **adversarially security-reviewed**; the real OIDC round-trip — real discovery, real RS256 JWKS validation, real `sub` binding, real `amr` — was **proven against a real authentik 2025.10.4** stood up by the bundle's own compose template, with the adapter unchanged.

---

When the first release is cut, the entries above move under a dated `## [X.Y.Z] - YYYY-MM-DD` heading and a
fresh `## [Unreleased]` section takes their place — see
[`CONTRIBUTING.md` → Changelog](./CONTRIBUTING.md#changelog).
