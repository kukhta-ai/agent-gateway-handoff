{
  "name": "@gla/kernel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure domain: entities, ports (interfaces), error taxonomy, caveat model. Zero third-party imports.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  }
}
